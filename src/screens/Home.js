import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Home = ({Name }) => {
  return (
    <View>
      <Text>{Name}</Text>
    </View>
  )
}

export default Home

const styles = StyleSheet.create({})